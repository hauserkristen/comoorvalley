export const currentAboutPage = (state) => state.about

export const currentHomePage = (state) => state.home

export const currentContactPage = (state) => state.contact

export const currentInfoPage = (state) => state.info

export const currentGalleryPage = (state) => state.gallery

export const currentNewsPage = (state) => state.news
