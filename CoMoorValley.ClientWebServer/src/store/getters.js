export const currentLogOutState = (state) => state.showLogOut

export const currentUserID = (state) => state.user.currentID

export const currentUserType = (state) => state.user.selectedType

export const currentAvailableTypes = (state) => state.user.availableTypes

export const currentNewEmployeeTypes = (state) => state.user.newEmployeeTypes

export const currentMenuItems = (state) => state.order.availableOptions

export const currentModalInfo = (state) => state.modal

export const notifications = (state) => state.order.notifications
